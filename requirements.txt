# PolyBot — Python dependencies
# Install with: pip install -r requirements.txt

# Async HTTP
aiohttp>=3.10.0

# FastAPI backend + ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Pydantic v2 (FastAPI dependency)
pydantic>=2.9.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Optional: for charting in paper trading reports
# matplotlib>=3.9.0  # uncomment if chart image generation is needed
